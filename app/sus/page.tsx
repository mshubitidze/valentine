export default async function SusPage() {
  await new Promise((resolve) => setTimeout(resolve, 5000));
  return (
    <main className="flex items-center justify-center min-h-screen">sus</main>
  );
}
